import 'package:flutter/material.dart';

class SetListScreen extends StatelessWidget {
  // TODO implicitly_animated_reorderable_listを使ってセットリストのUI作成
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('セットリスト作成'),
      ),
      body: Container(),
    );
  }
}
